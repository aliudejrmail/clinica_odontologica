# Configurações do Servidor
PORT=3333
NODE_ENV=development

# Segurança (use valores seguros em produção)
JWT_SECRET=altere_para_uma_chave_secreta_longa_e_aleatoria
# Chave para criptografia de CPF (LGPD). Use 64 caracteres hex (32 bytes) em produção.
# Se não definida, JWT_SECRET é usada (não recomendado em produção).
#
# Como gerar (rode no servidor ou máquina local e copie o resultado para o .env de produção):
#   node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
# Depois defina no .env: CPF_ENCRYPTION_KEY=valor_gerado_de_64_caracteres
#
# CPF_ENCRYPTION_KEY=

# Banco de Dados (substitua usuário, senha e nome do banco pelos seus)
DATABASE_URL="postgresql://USUARIO:SENHA@localhost:5432/NOME_DO_BANCO"

# Frontend (para CORS em produção)
FRONTEND_URL=http://localhost:3000

# Configurações de Email (opcional)
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_USER=seu_email@gmail.com
# SMTP_PASS=sua_senha
# SMTP_FROM=OdontoClínica <seu_email@gmail.com>

# Configurações de Backup (opcional)
# BACKUP_PATH=/backups/odonto
# BACKUP_RETENTION_DAYS=30

# Configurações de Rate Limit
RATE_LIMIT_MAX=100
RATE_LIMIT_WINDOW=1m

# Configurações de Upload (opcional)
# UPLOAD_MAX_SIZE=10485760 # 10MB
# UPLOAD_PATH=./uploads